version: 2

models:
  - name: dim_address
    description: ""
    config: 
      materialized: view
      alias: dim_address
      tags: []
    columns:
      - name: address_id
        description: integer
        # tests:
        #   - not_null
        #   - relationships:
        #       to: ref("dim_customer")
        #       field: customer_address_id